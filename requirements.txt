# Core pipeline
polars>=1.0
numpy>=1.26
pyarrow>=15.0
scikit-learn>=1.4

# Sequence model. Optional: tests skip if absent, and the paper's
# results do not depend on it.
torch>=2.2

# Preprocessing only (scripts/01_preprocess.py)
pandas>=2.1

# Tests
pytest>=8.0

# Figures
matplotlib>=3.8
